"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.setLocationHref = void 0;
const url = require("url");
const path = require("path");
/**
 * Sets the location href property on the global object.
 *
 * **Additional info**
 * Due to the deprecation of node-sass, it was decided to switch to sass (dart).
 * Current dart implementation uses global.location and it is undefined in Nodejs so the following workaround was used.
 *
 * **Related links**
 * Task: {@link https://cumulocity.atlassian.net/browse/MTM-44055 Get @c8y/cli working for ARM processor...},
 * Github: {@link https://github.com/dart-lang/sdk/issues/27979 Uri.base is broken...}
 */
function setLocationHref() {
    if (global && !global.location) {
        global.location = { href: `${url.pathToFileURL(process.cwd()).href}${path.sep}` };
    }
}
exports.setLocationHref = setLocationHref;
//# sourceMappingURL=Utils.js.map