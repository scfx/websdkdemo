import { NavigatorNode, NavigatorNodeFactory, Permissions } from '@c8y/ngx-components';
export declare class EcosystemNavigationFactory implements NavigatorNodeFactory {
    private permissions;
    private navs;
    private showNavigatorNodes;
    constructor(permissions: Permissions);
    get(): Promise<NavigatorNode[]>;
}
