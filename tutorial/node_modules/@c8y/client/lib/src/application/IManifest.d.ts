import { BillingMode, Isolation, IProvider, IApplication } from './';
export interface IManifest {
    id: number;
    applicationId: number;
    imports: string[];
    contextPath: string;
    version?: string;
    billingMode: BillingMode;
    isolation: Isolation;
    provider: IProvider;
    noAppSwitcher?: boolean;
    exports?: any;
    remotes?: ApplicationRemotePlugins;
    isPackage?: boolean;
    package?: 'application' | 'plugin';
    source?: string | number | IApplication;
    [key: string]: any;
}
export interface ApplicationRemotePlugins {
    [key: string]: string[];
}
