"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.BuildCommand = void 0;
const options_1 = require("../options");
const Commands_1 = require("./Commands");
const logUpdate = require("log-update");
const Utils_1 = require("../utils/Utils");
class BuildCommand extends Commands_1.Commands {
    constructor() {
        super(...arguments);
        this.name = 'build [appPaths...]';
        this.description = options_1.options['TXT.BUILD'];
        this.options = [
            {
                name: '-o, --output [outputPath]',
                description: options_1.options['TXT.BUILD.OUTPUTPATH']
            },
            {
                name: '--ci',
                description: options_1.options['TXT.SERVER.CI']
            }
        ];
        this.log = logUpdate.create(process.stdout);
    }
    async action(_appPaths, cmd) {
        Utils_1.setLocationHref();
        try {
            const appFolderGlobs = _appPaths.length ? _appPaths : options_1.options['PATH.APPS'];
            const apps = await this.getApps(appFolderGlobs);
            for (const app of apps) {
                console.info(`Building app ${app.contextPath}...`);
                if (cmd.branding) {
                    console.info(`with branding from ${cmd.branding}`);
                    app.branding = cmd.branding;
                }
                app.events.on('build.progress', () => this.log(app.getLogMsg()));
                const stats = await app.build(Object.assign({ mode: 'production' }, this.getWebpackExtraOptions()), this.getOption('ci'));
                this.log.done();
                if (stats.errors) {
                    console.error(stats.errors);
                }
                else {
                    console.info(stats.toString({
                        assets: false,
                        cached: false,
                        children: false,
                        chunks: false,
                        chunkModules: false,
                        colors: true,
                        hash: true,
                        modules: false,
                        reasons: false,
                        source: false,
                        timings: true,
                        version: true,
                        warnings: false
                    }));
                }
                if (stats.errors || (stats.compilation.errors && stats.compilation.errors.length)) {
                    process.exit(1);
                }
            }
        }
        catch (ex) {
            console.error(options_1.options['TXT.BUILD.FAILED']);
            console.error(ex);
            process.exit(1);
        }
    }
}
exports.BuildCommand = BuildCommand;
//# sourceMappingURL=BuildCommand.js.map