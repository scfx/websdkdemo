"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.config = void 0;
const webpack = require("webpack");
function config(env) {
    const entry = {};
    if (env.hmr) {
        entry.hmr = 'webpack-hot-middleware/client?reload=true';
    }
    return {
        entry,
        // watch: true, // https://github.com/webpack/webpack-cli/issues/1918#issuecomment-712128616
        watchOptions: {
            aggregateTimeout: 600,
            ignored: ['**/node_modules', '**/.git', '**/$$_lazy_route_resource']
        },
        plugins: [env.hmr && new webpack.HotModuleReplacementPlugin()].filter(Boolean)
    };
}
exports.config = config;
//# sourceMappingURL=development.js.map